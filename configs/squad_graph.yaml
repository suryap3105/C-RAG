
experiment_name: "squad_graph_rag"
description: "C-RAG on SQuAD using Constructed Knowledge Graph"

llm:
  provider: "mock"
  model: "llama3"

retrieval:
  k: 10
  type: "hybrid"
  use_reranker: true
  kg_path: "data/graphs/squad_kg.json"  # Point to local KG

agent:
  max_hops: 2
  max_expansions: 5
  reasoning: true
