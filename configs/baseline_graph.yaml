experiment_name: "baseline_graph_static"
description: "Static graph (non-agentic) baseline"

llm:
  provider: "mock"  # Use MockLLM for testing

retrieval:
  k: 5
  type: "hybrid"
  expansion_hops: 1 # static one-hop only
  use_reranker: true

agent:
  max_hops: 1 # Static expansion depth
  reasoning: false
