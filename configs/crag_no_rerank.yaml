experiment_name: "crag_no_rerank"
description: "C-RAG without ColBERT reranking (Key internal baseline)"

llm:
  provider: "mock"  # Use MockLLM for testing

retrieval:
  k: 10
  type: "hybrid"
  use_reranker: false

agent:
  max_hops: 3
  reasoning: true
