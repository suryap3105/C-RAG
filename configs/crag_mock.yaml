
experiment_name: "crag_mock_test"
description: "C-RAG with MockLLM for Testing"

llm:
  provider: "mock"  # Use MockLLM for unit testing

retrieval:
  k: 10
  type: "hybrid"
  use_reranker: true

agent:
  max_hops: 3
  max_expansions: 5
  reasoning: true
