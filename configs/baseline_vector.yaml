# Inherits defaults.yaml implicitly in loader (or manually here)
experiment_name: "baseline_vector"
description: "Vector-only RAG baseline"

llm:
  provider: "mock"  # Use MockLLM for testing
  
retrieval:
  k: 10
  type: "vector" # vector only

  use_reranker: false

agent:
  max_hops: 0 # Disable multi-hop
  reasoning: false
