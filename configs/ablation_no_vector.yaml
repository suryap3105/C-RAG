experiment_name: "ablation_no_vector"
description: "C-RAG without vector prefilter - pure graph traversal with reranking"

llm:
  provider: "mock"
  model: "llama3"

retrieval:
  k: 0  # Disable vector retrieval
  type: "graph_only"  # Pure graph expansion
  use_reranker: true

agent:
  max_hops: 3
  max_expansions: 5
  reasoning: true
