
experiment_name: "crag_full_scale"
description: "C-RAG Full Scale (146k Questions Knowledge Base)"

llm:
  provider: "mock"
  model: "llama3"

dataset: "squad" # Test on SQuAD portion

retrieval:
  k: 10
  type: "hybrid"
  use_reranker: true
  kg_path: "data/graphs/full_kg.json"  # Point to FULL KG

agent:
  max_hops: 3
  max_expansions: 5
  reasoning: true
  top_k_candidates: 5
